import 'package:flutter/material.dart';

const EdgeInsets bigEdge = EdgeInsets.all(10);
const EdgeInsets smallEdge = EdgeInsets.all(5);

const BorderRadius allround = BorderRadius.all(Radius.circular(10));

const TextStyle lightColorText = TextStyle(color: Colors.white, fontWeight: FontWeight.bold, fontSize: 20);
